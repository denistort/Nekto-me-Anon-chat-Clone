// export const debounce = (fn: Function, ms: number) => {
// 	let timeout;
// 	return function () {
// 		const fnCall = () => { fn.apply(this, arguments)}

// 		clearTimeout(timeout)
// 		timeout = setTimeout(fnCall, ms)
// 	}
// }

export const govno = 0;